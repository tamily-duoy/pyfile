3.1 什么是堆栈
串列形式的数据结构
只允许在链接串列或阵列的一端（堆叠顶端top）进行加入（push）、输出（pop）资料的运算。
另外，堆栈也可以用一维阵列或连结串列的形式来完成。
堆栈的另外一个相对的操作方式称为伫列。

由于堆栈数据结构只允许在一端进行操作，
因而按照后进先出（LIFO, Last In First Out）的原理运作。
3.2 特点
    先入后出，后入先出。
    除头尾节点之外，每个元素有一个前驱，一个后继。
3.3  操作
    top():获取堆栈顶端对象
    push():向栈里添加一个对象
    pop():从栈里推出一个对象
