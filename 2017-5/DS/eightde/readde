八大排序算法：和前面的每一个数比较，若之前数 > 当前位置， 则当前位置后移 。
    1、插入排序：（往前替换）
        两个指针，一个遍历、一个比较。
        在遍历的每个元素内，记住当前值，对前面所有元素相邻比较，大于则（大）相邻替换，直到找到最前位置，指定值。
    2、冒泡排序：（大元素后移）
        两个指针，一个遍历、一个比较。
        在遍历的每个元素内，相邻比较后面元素、更大元素往后移动。
    3、选择排序：（往后找出最小值）
        三个指针，一个遍历、一个往后比较、定位（最小）。
        在遍历的每个元素内，初始化定位（=该遍历指针），比较其后元素找到最小，更新定位指针。遍历、定位元素互换。
    4、希尔排序：
        指针
    5、
    6、
    7、
    8、